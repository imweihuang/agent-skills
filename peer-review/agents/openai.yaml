interface:
  display_name: "Peer Review"
  short_description: "Manually run a safe CLI or browser peer review"
  default_prompt: "Use $peer-review only when I explicitly request external review. Route explicit ChatGPT Pro or browser requests through the logged-in Chrome path and verify the visible selector before submitting. For Claude-family CLI reviews, routine uses Opus/high, judgment uses Fable 5/high, and load-bearing uses Fable 5/xhigh. Classification never authorizes invocation. There is no automatic fallback or Opus-to-Fable escalation; add reviewers or override the model only when I explicitly request them."
